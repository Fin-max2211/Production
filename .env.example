# ============================================================
# .env.example — ตัวอย่างการตั้งค่า Environment Variables
# ============================================================
# 📝 วิธีใช้:
#   1. Copy ไฟล์นี้แล้วเปลี่ยนชื่อเป็น .env
#   2. แก้ไขค่าตามต้องการ
#   3. ⚠️ ห้าม commit ไฟล์ .env ขึ้น Git!
# ============================================================

# ── Server Settings ──────────────────────────────────────────
PORT=3000
NODE_ENV=development

# ── Security Settings ────────────────────────────────────────
# จำกัดจำนวน request ต่อ IP (ต่อ 15 นาที)
RATE_LIMIT_MAX=100

# อนุญาต CORS จาก domain ไหน (ใส่ domain จริงตอน deploy)
# ตัวอย่าง: https://tu-starter-pack.com
ALLOWED_ORIGIN=

# API Key สำหรับเข้าถึง /api/stats (ตั้งเองเป็นอะไรก็ได้ ยาวๆ)
# ตัวอย่าง: my-super-secret-key-12345
ADMIN_API_KEY=

# ── Data Settings ────────────────────────────────────────────
# ชื่อไฟล์ Excel ที่เก็บข้อมูล
EXCEL_FILENAME=responses.xlsx

# ── Backup Settings ─────────────────────────────────────────
# เปิด/ปิดระบบ backup อัตโนมัติ (true/false)
BACKUP_ENABLED=true

# จำนวนไฟล์ backup สูงสุดที่เก็บไว้ (เก่าสุดจะถูกลบ)
BACKUP_MAX_FILES=10
